[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "resolution_week3_Waffl"
version = "0.0.1"
dependencies = [ "fastapi[standard]" ]

[project.scripts]
todo-api = "resolution_week3_YOUR_USERNAME.main:main"